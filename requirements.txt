# ---------------------------------------------------------------
# System requirement: Java 17 LTS (Temurin 17) must be installed
# for PySpark to work. Java 23+ removes APIs that PySpark requires.
#
# Install on macOS:
#   brew install --cask temurin@17
#
# Set JAVA_HOME (add to ~/.zshrc to persist):
#   export JAVA_HOME=$(/usr/libexec/java_home -v 17)
# ---------------------------------------------------------------
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
seaborn>=0.12.0
scipy>=1.10.0
ucimlrepo>=0.0.7
scikit-learn>=1.3.0
mlxtend>=0.23.0
pyspark>=3.4.0
